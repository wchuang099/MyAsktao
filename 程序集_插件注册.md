---
title: "程序集_插件注册"
weight: 21
---
.版本 2
.支持库 spec

.程序集 程序集_插件注册

.子程序 插件注册_大漠注册, 逻辑型
.局部变量 插件版本, 文本型
.局部变量 注册结果, 整数型


' 调用大漠插件第一步  把dm插件注册到系统里面去    注册到系统又分为两类   免注册  和 本机com组件注册
' 调试输出 (“大漠插件注册到系统的结果”, 注册COM组件 (取运行目录 () ＋ “\资源\dm.dll”))  ' 返回值是逻辑型  返回真就代表着成功注册到系统  返回假  注册到系统失败了
' 免注册大漠  需要一个dll的支持！


' 大漠内存注册 (取运行目录 () ＋ “\资源\dm.dll”)   大漠内存注册到系统
' 免注册大漠 (取运行目录 () ＋ “\资源\DmReg.dll”, 取运行目录 () ＋ “\资源\dm.dll”, )‘  免注册大漠调用

.如果 (大漠内存注册 (取运行目录 () ＋ “\资源\dm.dll”))  ' <!!!>
    ' 第二步  创建大漠对象
    .如果 (大漠.创建对象 ())
        ' 第三步  获取一下当前大漠的版本号
        插件版本 ＝ 大漠.取插件版本 ()
        .如果 (插件版本 ≠ “”)
            调试输出 (“当前大漠插件版本”, 插件版本)
            ' 第四步  需要 注册大漠收费插件
            注册结果 ＝ 大漠.注册VIP (“jiubanstudio9f76ced6355932fa9e918323acdc278e”, “laoziniubi”)
            .如果 (注册结果 ＝ 1)
                调试输出 (“注册大漠插件成功”)
                大漠.置全局路径 (取运行目录 () ＋ “\资源\”)
                返回 (真)
            .否则
                ' 注册失败   失败原因？
                .判断开始 (注册结果 ＝ -1)
                    信息框 (“连接网络失败”, #警告图标, “ 插件注册失败”, _启动窗口)
                .判断 (注册结果 ＝ -2)
                    信息框 (“进程没有以管理员方式运行. (出现在win7 win8 vista 2008.建议关闭uac)”, #警告图标, “ 插件注册失败”, _启动窗口)
                .默认
                    信息框 (“注册大漠插件失败,错误码：” ＋ 到文本 (注册结果), #警告图标, “ 插件注册失败”, _启动窗口)
                .判断结束

            .如果结束

        .否则
            信息框 (“获取大漠插件版本失败”, #警告图标, “ 插件注册失败”, _启动窗口)
        .如果结束

    .否则
        信息框 (“创建大漠插件失败”, #警告图标, “ 插件注册失败”, _启动窗口)
    .如果结束

.否则
    信息框 (“注册大漠插件到系统失败”, #警告图标, “ 插件注册失败”, _启动窗口)
.如果结束



返回 (假)


.子程序 大漠内存注册, 逻辑型, 公开
.参数 路径, 文本型
.局部变量 路径文本, 文本型
.局部变量 模块句柄, 整数型
.局部变量 输出函数地址, 整数型

路径文本 ＝ 取空白文本 (255)
取短文件名 (路径, 路径文本, 255)
模块句柄 ＝ 载入动态链接库 (路径文本)
.如果真 (模块句柄 ＝ 0)
    返回 (假)
.如果真结束
输出函数地址 ＝ GetProcAddress (模块句柄, “DllRegisterServer”)
.如果真 (输出函数地址 ≤ 0)
    释放动态链接库 (模块句柄)
    返回 (假)
.如果真结束
CallWindowProc (输出函数地址, 0, 0, 0, 0)
释放动态链接库 (模块句柄)
返回 (真)

.子程序 大漠_多线程创建对象, 逻辑型
.参数 i, 整数型
.局部变量 版本号, 文本型


.如果 (全局_信息 [i].大漠.创建对象 ())
    版本号 ＝ 全局_信息 [i].大漠.取插件版本 ()
    .如果 (版本号 ≠ “”)
        ' 设置当前线程大漠的  全局路径
        调试输出 (“当前窗口” ＋ 到文本 (i) ＋ “号窗口 大漠版本：” ＋ 版本号)
        全局_信息 [i].大漠.置全局路径 (取运行目录 () ＋ “\资源\”)

        全局_信息 [i].大漠.字库_置文件 (0, “sb.txt”)  ' 零号字库  鼠标字库
        全局_信息 [i].大漠.字库_置文件 (1, “对话框对话.txt”)  ' 1号字库  对话框
        全局_信息 [i].大漠.字库_置文件 (2, “左上角地图名.txt”)  ' 2号  左上角地图名

        ' 字库   都在这边声明
        返回 (真)

    .否则
        调试输出 (“窗口” ＋ 到文本 (i) ＋ “获取大漠插件版本失败”)
    .如果结束

.否则
    调试输出 (“窗口” ＋ 到文本 (i) ＋ “创建大漠对象失败”)
.如果结束



返回 (假)

.子程序 绑定窗口, 逻辑型
.参数 i, 整数型
.局部变量 执行流程, 文本型
.局部变量 用户设置的窗口名, 文本型
.局部变量 绑定结果, 整数型

执行流程 ＝ “查找指定窗口”
.判断循环首 (真)
    .判断开始 (执行流程 ＝ “查找指定窗口”)  ' 防止除了一种模式 比如 uu  vnc
        用户设置的窗口名 ＝ 读配置项 (取运行目录 () ＋ “\配置\用户设置.ini”, “用户设置” ＋ 到文本 (i), “VNC名称”, )
        .如果 (用户设置的窗口名 ≠ “”)
            全局_信息 [i].窗口句柄 ＝ 到整数 (全局_信息 [i].大漠.窗口_枚举句柄 (0, 用户设置的窗口名, “vwr::CDesktopWin”, 35))
            .如果 (全局_信息 [i].窗口句柄 ＞ 0)
                调试输出 (“vnc窗口句柄：”, 全局_信息 [i].窗口句柄)
                执行流程 ＝ “绑定窗口”
            .否则
                调试输出 (“绑定窗口失败->获取窗口句柄失败”)
                返回 (假)
            .如果结束



        .否则
            ' 为空才会执行到此处
            调试输出 (“绑定窗口失败->用户未设置窗口名”)
            返回 (假)
        .如果结束


    .判断 (执行流程 ＝ “绑定窗口”)
        ' 第一步  判断下 窗口是否最小化      窗口大小是否符合规范   不然向后 执行会导致报错

        全局_信息 [i].大漠.窗口_置大小 (全局_信息 [i].窗口句柄, 1040, 807)  ' 可能导致识别报错
        .如果 (窗口_是否最小化 (全局_信息 [i].窗口句柄) ≠ 真)  ' 最小化 该子程序返回真   没有最小化 返回假
            ' 绑定窗口

            绑定结果 ＝ 全局_信息 [i].大漠.绑定Ex (全局_信息 [i].窗口句柄, “gdi”, “dx”, “windows”, “”, 0)
            .如果 (绑定结果 ＝ 1)
                调试输出 (“绑定窗口成功”)
                全局_信息 [i].大漠.鼠标_模拟真实_VIP (2, 5, 100)

                返回 (真)

            .否则
                调试输出 (“绑定窗口失败”)
                返回 (假)
            .如果结束

        .否则
            调试输出 (“当前窗口最小化了”)
            全局_信息 [i].大漠.窗口_置状态 (全局_信息 [i].窗口句柄, 12)
            延时 (500)
        .如果结束

    .默认

    .判断结束


    延时 (500)
    处理事件 ()
.判断循环尾 ()
返回 (假)
