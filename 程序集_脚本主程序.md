---
title: "程序集_脚本主程序"
weight: 23
---
.版本 2
.支持库 spec

.程序集 程序集_脚本主程序

.子程序 主程序
.参数 i, 整数型
.局部变量 执行流程, 文本型

' 大漠多线程对象  图灵  一些com对象   线程开头的时候 一定要初始化com库
线程_初始化COM库 ()
' -------一些执行代码----------
执行流程 ＝ “创建大漠线程对象”
.判断循环首 (真)
    .判断开始 (执行流程 ＝ “创建大漠线程对象”)
        ' 创建大漠多线程对象
        .如果 (大漠_多线程创建对象 (i))
            执行流程 ＝ “绑定窗口”
        .否则
            调试输出 (“主程序->大漠多线程对象创建失败”)
            返回 ()
        .如果结束

    .判断 (执行流程 ＝ “绑定窗口”)
        .如果 (绑定窗口 (i))
            执行流程 ＝ “分配任务”
        .否则
            调试输出 (“主程序->绑定窗口失败”)
            返回 ()
        .如果结束

    .判断 (执行流程 ＝ “分配任务”)
        任务执行分配 (i)
    .默认

    .判断结束



    延时 (500)
    处理事件 ()
.判断循环尾 ()






