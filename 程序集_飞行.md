---
title: "程序集_飞行"
weight: 23
---
.版本 2
.支持库 spec

.程序集 程序集_飞行

.子程序 飞行旗_长寿, 逻辑型
.参数 i, 整数型
.参数 参数_坐标x, 整数型, , 小地图的坐标
.参数 参数_坐标y, 整数型, , 小地图的坐标
.局部变量 执行流程, 文本型
.局部变量 背包识别范围, 坐标类
.局部变量 旗子坐标, 坐标类
.局部变量 枚举小红点, 文本型
.局部变量 小红点集, 文本型, , "0"
.局部变量 索引, 整数型
.局部变量 最近坐标, 坐标类
.局部变量 小红点坐标, 文本型, , "0"
.局部变量 点击坐标, 坐标类
.局部变量 屏幕坐标换算, 坐标类
.局部变量 代价, 整数型
.局部变量 现行代价, 双精度小数型
.局部变量 地图位置, 地图数据
.局部变量 临时地图位置, 地图数据


执行流程 ＝ “判断飞行棋是否打开”
.判断循环首 (真)
    .判断开始 (执行流程 ＝ “判断飞行棋是否打开”)
        .如果 (全局_信息 [i].大漠.找图Ex (245, 132, 426, 215, “飞行旗_长寿村_标识.bmp”, “030303”, 0.9, 0) ＝ “”)
            .判断开始 (界面判定_背包是否打开 (i, 背包识别范围, ))
                .如果 (全局_信息 [i].大漠.找图 (背包识别范围.x, 背包识别范围.y, 背包识别范围.x1, 背包识别范围.y1, “飞行旗_绿色.bmp”, “030303”, 0.9, 0, 旗子坐标.x, 旗子坐标.y) ≠ -1)

                    梦幻鼠标移动 (i, 旗子坐标.x, 旗子坐标.y, , )
                    延时 (50)
                    全局_信息 [i].大漠.鼠标_右键单击 ()
                    .计次循环首 (50, )
                        .如果真 (全局_信息 [i].大漠.找图Ex (245, 132, 426, 215, “飞行旗_长寿村_标识.bmp”, “030303”, 0.9, 0) ≠ “”)
                            执行流程 ＝ “飞旗”

                            跳出循环 ()
                        .如果真结束

                        延时 (100)
                    .计次循环尾 ()
                .否则
                    调试输出 (“未找到旗子”)
                    延时 (1000000)
                .如果结束



            .默认
                打开_背包 (i)
            .判断结束

        .否则
            执行流程 ＝ “飞旗”
        .如果结束

    .判断 (执行流程 ＝ “飞旗”)
        ' 参数屏幕坐标转换
        屏幕坐标换算.x ＝ 参数_坐标x × 1.75 ＋ 269  ' 把小地图的坐标  换算成我们屏幕坐标
        屏幕坐标换算.y ＝ 496 － 参数_坐标y × 1.66


        枚举小红点 ＝ 全局_信息 [i].大漠.找图Ex (251, 131, 564, 517, “飞行旗_小红点.bmp”, “030303”, 0.9, 0)
        .如果 (枚举小红点 ≠ “”)
            ' 此时 我们已经有所有的飞行旗小红点了!
            小红点集 ＝ 分割文本 (枚举小红点, “|”, )  ' 分割 大漠格式    找图ex返回格式   0,100,100|0,200,200.....   索引,x坐标,y坐标|索引,x坐标,y坐标
            代价 ＝ 99999
            .计次循环首 (取数组成员数 (小红点集), 索引)
                小红点坐标 ＝ 分割文本 (小红点集 [索引], “,”, )

                调试输出 (取绝对值 (到整数 (小红点坐标 [2]) － 屏幕坐标换算.x), 取绝对值 (到整数 (小红点坐标 [3]) － 屏幕坐标换算.y), 取绝对值 (取绝对值 (到整数 (小红点坐标 [2]) － 屏幕坐标换算.x) ＋ 取绝对值 (到整数 (小红点坐标 [3]) － 屏幕坐标换算.y)), “要抵达的位置”, 屏幕坐标换算.x, 屏幕坐标换算.y, “现行位置”, 小红点坐标 [2], 小红点坐标 [3])

                现行代价 ＝ 取绝对值 (取绝对值 (到整数 (小红点坐标 [2]) － 屏幕坐标换算.x) ＋ 取绝对值 (到整数 (小红点坐标 [3]) － 屏幕坐标换算.y))
                .如果真 (现行代价 ≤ 代价)
                    ' 代价低的  就替换  一直循环  保留代价最低的
                    代价 ＝ 现行代价
                    点击坐标.x ＝ 到整数 (小红点坐标 [2])
                    点击坐标.y ＝ 到整数 (小红点坐标 [3])
                .如果真结束

            .计次循环尾 ()
            调试输出 (“最终点击小地图位置”, 点击坐标.x, 点击坐标.y)
            .如果 (地图_获取当前位置 (i, 临时地图位置))
                执行流程 ＝ “判断是否抵达区域”
            .否则
                调试输出 (“长寿村飞行棋->获取地图失败”)
            .如果结束


        .否则
            调试输出 (“疑是飞行旗未打开”)
            执行流程 ＝ “判断飞行棋是否打开”
        .如果结束

    .判断 (执行流程 ＝ “判断是否抵达区域”)
        .如果 (全局_信息 [i].大漠.找图Ex (245, 132, 426, 215, “飞行旗_长寿村_标识.bmp”, “030303”, 0.9, 0) ＝ “”)
            ' 已经点击完毕了
            .如果 (地图_获取当前位置 (i, 地图位置))
                .如果 (地图位置.地图名 ＝ “长寿村” 且 临时地图位置.地图坐标x ≠ 地图位置.地图坐标x 且 临时地图位置.地图坐标y ≠ 地图位置.地图坐标y)
                    ' 取绝对值 (地图位置.地图坐标x － 参数_坐标x) ≤ 15 且 取绝对值 (地图位置.地图坐标y － 参数_坐标y) ≤ 15
                    ' 临时地图位置.地图坐标x ≠ 地图位置.地图坐标x 且 临时地图位置.地图坐标y ≠ 地图位置.地图坐标y
                    ' 多这个判断  是因为外部调用是  某些任务 或者环境导致  我们需要再原地图飞
                    关闭_背包 (i)
                    调试输出 (“抵达长寿村”)
                    返回 (真)
                .否则
                    调试输出 (“等待抵达” ＋ 地图位置.地图名 ＋ “-” ＋ 到文本 (参数_坐标x) ＋ “,” ＋ 到文本 (参数_坐标y))
                .如果结束


            .否则
                调试输出 (“长寿村飞行棋->获取地图失败”)

            .如果结束

        .否则
            梦幻鼠标移动 (i, 点击坐标.x, 点击坐标.y, , )
            延时 (50)
            全局_信息 [i].大漠.鼠标_左键单击 ()
            延时 (500)
        .如果结束

    .默认

    .判断结束

    延时 (500)
.判断循环尾 ()

返回 (假)

.子程序 飞行旗_长安酒店, 逻辑型
.参数 i, 整数型
.参数 参数_坐标x, 整数型, , 小地图的坐标
.参数 参数_坐标y, 整数型, , 小地图的坐标
.局部变量 执行流程, 文本型
.局部变量 背包识别范围, 坐标类
.局部变量 旗子坐标, 坐标类
.局部变量 枚举小红点, 文本型
.局部变量 小红点集, 文本型, , "0"
.局部变量 索引, 整数型
.局部变量 最近坐标, 坐标类
.局部变量 小红点坐标, 文本型, , "0"
.局部变量 点击坐标, 坐标类
.局部变量 屏幕坐标换算, 坐标类
.局部变量 代价, 整数型
.局部变量 现行代价, 双精度小数型
.局部变量 地图位置, 地图数据
.局部变量 临时地图位置, 地图数据


执行流程 ＝ “判断飞行棋是否打开”
.判断循环首 (真)
    .判断开始 (执行流程 ＝ “判断飞行棋是否打开”)
        .如果 (全局_信息 [i].大漠.找图Ex (108, 159, 322, 274, “飞行旗_长安酒店旗_标识.bmp”, “030303”, 0.9, 0) ＝ “”)
            .判断开始 (界面判定_背包是否打开 (i, 背包识别范围, ))
                .如果 (全局_信息 [i].大漠.找图 (背包识别范围.x, 背包识别范围.y, 背包识别范围.x1, 背包识别范围.y1, “飞行旗_红色.bmp”, “030303”, 0.9, 0, 旗子坐标.x, 旗子坐标.y) ≠ -1)
                    关闭_背包 (i)
                    梦幻鼠标移动 (i, 旗子坐标.x, 旗子坐标.y, , )
                    延时 (50)
                    全局_信息 [i].大漠.鼠标_右键单击 ()

                    .计次循环首 (50, )
                        .如果真 (全局_信息 [i].大漠.找图Ex (108, 159, 322, 274, “飞行旗_长安酒店旗_标识.bmp”, “030303”, 0.9, 0) ≠ “”)
                            执行流程 ＝ “飞旗”
                            跳出循环 ()
                        .如果真结束

                        延时 (100)
                    .计次循环尾 ()
                .否则
                    调试输出 (“未找到旗子”)
                    延时 (1000000)
                .如果结束



            .默认
                打开_背包 (i)
            .判断结束

        .否则
            执行流程 ＝ “飞旗”
        .如果结束

    .判断 (执行流程 ＝ “飞旗”)
        ' 参数屏幕坐标转换
        屏幕坐标换算.x ＝ 参数_坐标x × 1.5 ＋ 130  ' 把小地图的坐标  换算成我们屏幕坐标
        屏幕坐标换算.y ＝ 459 － 参数_坐标y × 1.2


        枚举小红点 ＝ 全局_信息 [i].大漠.找图Ex (116, 178, 685, 475, “飞行旗_小红点.bmp”, “030303”, 0.9, 0)
        .如果 (枚举小红点 ≠ “”)
            ' 此时 我们已经有所有的飞行旗小红点了!
            小红点集 ＝ 分割文本 (枚举小红点, “|”, )  ' 分割 大漠格式    找图ex返回格式   0,100,100|0,200,200.....   索引,x坐标,y坐标|索引,x坐标,y坐标
            代价 ＝ 99999
            .计次循环首 (取数组成员数 (小红点集), 索引)
                小红点坐标 ＝ 分割文本 (小红点集 [索引], “,”, )

                调试输出 (取绝对值 (到整数 (小红点坐标 [2]) － 屏幕坐标换算.x), 取绝对值 (到整数 (小红点坐标 [3]) － 屏幕坐标换算.y), 取绝对值 (取绝对值 (到整数 (小红点坐标 [2]) － 屏幕坐标换算.x) ＋ 取绝对值 (到整数 (小红点坐标 [3]) － 屏幕坐标换算.y)), “要抵达的位置”, 屏幕坐标换算.x, 屏幕坐标换算.y, “现行位置”, 小红点坐标 [2], 小红点坐标 [3])

                现行代价 ＝ 取绝对值 (取绝对值 (到整数 (小红点坐标 [2]) － 屏幕坐标换算.x) ＋ 取绝对值 (到整数 (小红点坐标 [3]) － 屏幕坐标换算.y))
                .如果真 (现行代价 ≤ 代价)
                    ' 代价低的  就替换  一直循环  保留代价最低的
                    代价 ＝ 现行代价
                    点击坐标.x ＝ 到整数 (小红点坐标 [2])
                    点击坐标.y ＝ 到整数 (小红点坐标 [3])
                .如果真结束

            .计次循环尾 ()
            调试输出 (“最终点击小地图位置”, 点击坐标.x, 点击坐标.y)
            .如果 (地图_获取当前位置 (i, 临时地图位置))
                执行流程 ＝ “判断是否抵达区域”
            .否则
                调试输出 (“长安飞行棋->获取地图失败”)
            .如果结束


        .否则
            调试输出 (“疑是飞行旗未打开”)
            执行流程 ＝ “判断飞行棋是否打开”
        .如果结束

    .判断 (执行流程 ＝ “判断是否抵达区域”)
        .如果 (全局_信息 [i].大漠.找图Ex (108, 159, 322, 274, “飞行旗_长安酒店旗_标识.bmp”, “030303”, 0.9, 0) ＝ “”)
            ' 已经点击完毕了
            .如果 (地图_获取当前位置 (i, 地图位置))
                .如果 (地图位置.地图名 ＝ “长安城” 且 临时地图位置.地图坐标x ≠ 地图位置.地图坐标x 且 临时地图位置.地图坐标y ≠ 地图位置.地图坐标y)
                    ' 取绝对值 (地图位置.地图坐标x － 参数_坐标x) ≤ 15 且 取绝对值 (地图位置.地图坐标y － 参数_坐标y) ≤ 15
                    ' 临时地图位置.地图坐标x ≠ 地图位置.地图坐标x 且 临时地图位置.地图坐标y ≠ 地图位置.地图坐标y
                    ' 多这个判断  是因为外部调用是  某些任务 或者环境导致  我们需要再原地图飞

                    调试输出 (“抵达长安城”)
                    返回 (真)
                .否则
                    调试输出 (“等待抵达” ＋ 地图位置.地图名 ＋ “-” ＋ 到文本 (参数_坐标x) ＋ “,” ＋ 到文本 (参数_坐标y))
                .如果结束


            .否则
                调试输出 (“长安飞行棋->获取地图失败”)

            .如果结束

        .否则
            梦幻鼠标移动 (i, 点击坐标.x, 点击坐标.y, , )
            延时 (50)
            全局_信息 [i].大漠.鼠标_左键单击 ()
            延时 (500)
        .如果结束

    .默认

    .判断结束

    延时 (500)
.判断循环尾 ()

返回 (假)

.子程序 飞行符, 逻辑型
.参数 i, 整数型
.参数 飞行旗位置, 文本型
.局部变量 执行流程, 文本型
.局部变量 抵达坐标, 坐标类
.局部变量 背包识别范围, 坐标类
.局部变量 旗子坐标, 坐标类
.局部变量 地图位置, 地图数据

执行流程 ＝ “判断飞行棋是否打开”
.判断循环首 (真)
    .判断开始 (执行流程 ＝ “判断飞行棋是否打开”)
        .如果 (全局_信息 [i].大漠.找图Ex (446, 246, 558, 339, “飞行符标识.bmp”, “030303”, 0.9, 0) ＝ “”)
            .判断开始 (界面判定_背包是否打开 (i, 背包识别范围, ))
                .如果 (全局_信息 [i].大漠.找图 (背包识别范围.x, 背包识别范围.y, 背包识别范围.x1, 背包识别范围.y1, “飞行符.bmp”, “030303”, 0.9, 0, 旗子坐标.x, 旗子坐标.y) ≠ -1)
                    梦幻鼠标移动 (i, 旗子坐标.x, 旗子坐标.y, , )
                    延时 (50)
                    全局_信息 [i].大漠.鼠标_右键单击 ()

                    .计次循环首 (50, )
                        .如果真 (全局_信息 [i].大漠.找图Ex (446, 246, 558, 339, “飞行符标识.bmp”, “030303”, 0.9, 0) ≠ “”)
                            执行流程 ＝ “飞旗”

                            跳出循环 ()
                        .如果真结束

                        延时 (100)
                    .计次循环尾 ()
                .否则
                    调试输出 (“未找到飞行符”)
                    延时 (1000000)
                .如果结束



            .默认
                打开_背包 (i)
            .判断结束

        .否则
            执行流程 ＝ “飞旗”
        .如果结束

    .判断 (执行流程 ＝ “飞旗”)
        .如果真 (寻找文本 (飞行旗位置, #飞行符_长安, , 假) ≠ -1)
            抵达坐标.x ＝ 519
            抵达坐标.y ＝ 298
        .如果真结束
        .如果真 (寻找文本 (飞行旗位置, #飞行符_长寿村, , 假) ≠ -1)
            抵达坐标.x ＝ 304
            抵达坐标.y ＝ 206
        .如果真结束
        .如果真 (寻找文本 (飞行旗位置, #飞行符_宝象国, , 假) ≠ -1)
            抵达坐标.x ＝ 292
            抵达坐标.y ＝ 348
        .如果真结束
        .如果真 (寻找文本 (飞行旗位置, #飞行符_朱紫国, , 假) ≠ -1)
            抵达坐标.x ＝ 351
            抵达坐标.y ＝ 417
        .如果真结束
        .如果真 (寻找文本 (飞行旗位置, #飞行符_傲来国, , 假) ≠ -1)
            抵达坐标.x ＝ 684
            抵达坐标.y ＝ 440
        .如果真结束
        .如果真 (寻找文本 (飞行旗位置, #飞行符_建邺城, , 假) ≠ -1)
            抵达坐标.x ＝ 532
            抵达坐标.y ＝ 378
        .如果真结束
        .如果真 (寻找文本 (飞行旗位置, #飞行符_西梁女国, , 假) ≠ -1)
            抵达坐标.x ＝ 299
            抵达坐标.y ＝ 267
        .如果真结束

        .如果 (抵达坐标.x ＞ 0 且 抵达坐标.y ＞ 0)
            执行流程 ＝ “前往指定地点”

        .否则
            调试输出 (“飞行符位置错误”)
        .如果结束

    .判断 (执行流程 ＝ “前往指定地点”)
        .如果 (全局_信息 [i].大漠.找图Ex (446, 246, 558, 339, “飞行符标识.bmp”, “030303”, 0.9, 0) ＝ “”)
            ' 已经点击完毕了
            .如果 (地图_获取当前位置 (i, 地图位置))
                .如果 (地图位置.地图名 ＝ 飞行旗位置)
                    调试输出 (“抵达” ＋ 飞行旗位置)
                    关闭_背包 (i)
                    返回 (真)
                .否则
                    调试输出 (“等待抵达” ＋ 飞行旗位置)
                .如果结束


            .否则
                调试输出 (“长寿村飞行棋->获取地图失败”)

            .如果结束

        .否则

            梦幻鼠标移动 (i, 抵达坐标.x, 抵达坐标.y, , )
            延时 (50)
            全局_信息 [i].大漠.鼠标_左键单击 ()
            延时 (500)
        .如果结束

    .默认

    .判断结束

    延时 (500)
.判断循环尾 ()
返回 (假)
