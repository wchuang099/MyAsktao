---
title: "程序集_插件识别"
weight: 20
---
.版本 2
.支持库 spec

.程序集 程序集_插件识别

.子程序 opencv_登录, 逻辑型

.如果真 (登录 (“glhjyj”, “123456”))
    调试输出 (“登录成功”)
    返回 (真)
.如果真结束

调试输出 (“登录失败”)

返回 (假)


.子程序 opencv_找图ex, 文本型
.参数 i, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 x1, 整数型
.参数 y1, 整数型
.参数 要找的图, 文本型
.参数 精准度, 小数型
.局部变量 大图数据, 字节集
.局部变量 小图数据, 字节集
.局部变量 信度, 双精度小数型
.局部变量 找到位置, 点型
.局部变量 图片集, 文本型, , "0"
.局部变量 索引, 整数型
.局部变量 查找结果, 文本型
.局部变量 intx, 整数型
.局部变量 inty, 整数型

全局_信息 [i].大漠.截图 (x, y, x1, y1, 取运行目录 () ＋ “\资源\” ＋ 到文本 (i) ＋ “_opencv区域截图.bmp”)
大图数据 ＝ 读入文件 (取运行目录 () ＋ “\资源\” ＋ 到文本 (i) ＋ “_opencv区域截图.bmp”)
' 判断下 图片是否存在  防止崩溃
图片集 ＝ 分割文本 (要找的图, “|”, )
.计次循环首 (取数组成员数 (图片集), 索引)  ' 索引是从1开始的
    .如果 (文件_是否存在 (取运行目录 () ＋ “\资源\” ＋ 图片集 [索引]) ≠ 真)
        信息框 (“未找到” ＋ 要找的图 ＋ “图片”, #警告图标, “ 资源缺失”, _启动窗口)
    .否则
        小图数据 ＝ 读入文件 (取运行目录 () ＋ “\资源\” ＋ 图片集 [索引])
        信度 ＝ 找图 (大图数据, 取字节集长度 (大图数据), 小图数据, 取字节集长度 (小图数据), 找到位置)
        ' 信度   找出来的图片  相识度是多少
        .如果真 (信度 ≥ 精准度)
            intx ＝ 找到位置.x ＋ x  ' +x 是因为用户区域 并非是从0 点起始值
            inty ＝ 找到位置.y ＋ y  ' +y 是因为用户区域 并非是从0 点起始值
            .如果 (查找结果 ＝ “”)
                查找结果 ＝ 到文本 (索引 － 1) ＋ “,” ＋ 到文本 (intx) ＋ “,” ＋ 到文本 (inty)
            .否则
                查找结果 ＝ 查找结果 ＋ “|” ＋ 到文本 (索引 － 1) ＋ “,” ＋ 到文本 (intx) ＋ “,” ＋ 到文本 (inty)
            .如果结束

        .如果真结束

    .如果结束

.计次循环尾 ()


返回 (查找结果)

.子程序 opencv_找图, 整数型
.参数 i, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 x1, 整数型
.参数 y1, 整数型
.参数 要找的图, 文本型
.参数 精准度, 小数型
.参数 intx, 整数型, 参考
.参数 inty, 整数型, 参考
.局部变量 大图数据, 字节集
.局部变量 小图数据, 字节集
.局部变量 信度, 双精度小数型
.局部变量 找到位置, 点型
.局部变量 图片集, 文本型, , "0"
.局部变量 索引, 整数型

全局_信息 [i].大漠.截图 (x, y, x1, y1, 取运行目录 () ＋ “\资源\” ＋ 到文本 (i) ＋ “_opencv区域截图.bmp”)
大图数据 ＝ 读入文件 (取运行目录 () ＋ “\资源\” ＋ 到文本 (i) ＋ “_opencv区域截图.bmp”)
' 判断下 图片是否存在  防止崩溃
图片集 ＝ 分割文本 (要找的图, “|”, )
.计次循环首 (取数组成员数 (图片集), 索引)  ' 索引是从1开始的
    .如果 (文件_是否存在 (取运行目录 () ＋ “\资源\” ＋ 图片集 [索引]) ≠ 真)
        信息框 (“未找到” ＋ 要找的图 ＋ “图片”, #警告图标, “ 资源缺失”, _启动窗口)
    .否则
        小图数据 ＝ 读入文件 (取运行目录 () ＋ “\资源\” ＋ 图片集 [索引])
        信度 ＝ 找图 (大图数据, 取字节集长度 (大图数据), 小图数据, 取字节集长度 (小图数据), 找到位置)
        ' 信度   找出来的图片  相识度是多少
        .如果真 (信度 ≥ 精准度)
            intx ＝ 找到位置.x ＋ x  ' +x 是因为用户区域 并非是从0 点起始值
            inty ＝ 找到位置.y ＋ y  ' +y 是因为用户区域 并非是从0 点起始值
            返回 (索引 － 1)  ' 跟大漠返回值 对齐
        .如果真结束

    .如果结束

.计次循环尾 ()


返回 (-1)

.子程序 文字_单识别, 文本型, 公开
.参数 i, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 x1, 整数型
.参数 y1, 整数型
.局部变量 返回指针, 整数型
.局部变量 返回长度, 整数型
.局部变量 返回结果, 文本型
.局部变量 图片, 字节集
.局部变量 图片存放位置, 文本型

图片存放位置 ＝ 取运行目录 () ＋ “\资源\单识别_窗口” ＋ 到文本 (i) ＋ “.bmp”  ' 注意下哈~~ 到文本 (i) ＋ “.bmp”     防止线程冲突
' 运行OCR缓存截图-里面图片可清空   文件夹
全局_信息 [i].大漠.截图 (x, y, x1, y1, 图片存放位置)
图片 ＝ 读入文件 (图片存放位置)
OcrOne (全局_模型指针, 图片, 取字节集长度 (图片), 返回指针, 返回长度)
返回结果 ＝ 到文本 (指针到字节集 (返回指针, 返回长度))
Delete_Chars (返回指针)
返回 (返回结果)

.子程序 文字_识别定位, 文本型, 公开
.参数 i, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 x1, 整数型
.参数 y1, 整数型
.局部变量 返回指针, 整数型
.局部变量 返回长度, 整数型
.局部变量 返回结果, 文本型
.局部变量 图片, 字节集
.局部变量 xh, 整数型
.局部变量 数量, 整数型
.局部变量 数据指针, 整数型
.局部变量 结果拆分, 文本型, , "0"
.局部变量 最终结果, 文本型
.局部变量 图片存放位置, 文本型


图片存放位置 ＝ 取运行目录 () ＋ “\资源\单识别_窗口” ＋ 到文本 (i) ＋ “.bmp”  ' 注意下哈~~ 到文本 (i) ＋ “.bmp”     防止线程冲突
' 运行OCR缓存截图-里面图片可清空   文件夹
全局_信息 [i].大漠.截图 (x, y, x1, y1, 图片存放位置)
图片 ＝ 读入文件 (图片存放位置)
数量 ＝ Ocr (全局_模型指针, 假, 图片, 取字节集长度 (图片), 数据指针)
.计次循环首 (数量, xh)
    GetDetectResult (数据指针, xh － 1, 返回指针, 返回长度)
    返回结果 ＝ 到文本 (指针到字节集 (返回指针, 返回长度))
    Delete_Chars (返回指针)
    结果拆分 ＝ 分割文本 (返回结果, “|”, )
    .如果真 (取数组成员数 (结果拆分) ≥ 10)
        .如果 (最终结果 ＝ “”)
            最终结果 ＝ 结果拆分 [10]
        .否则
            最终结果 ＝ 最终结果 ＋ 结果拆分 [10]
        .如果结束

    .如果真结束

.计次循环尾 ()


' 返回结果 ＝ 到文本 (指针到字节集 (返回指针, 返回长度))
返回 (最终结果)
