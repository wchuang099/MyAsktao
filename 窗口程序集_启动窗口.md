---
title: "程序集_启动窗口"
weight: 23
---
.版本 2
.支持库 spec
.支持库 iext

.程序集 窗口程序集_启动窗口

.子程序 _功能设置_被选择

载入 (窗口_功能设置, _启动窗口, 真)


.子程序 _超级列表框_中控台_右键单击表项

调试输出 (超级列表框_中控台.现行选中项 ＋ 1)
.如果真 (超级列表框_中控台.现行选中项 ≠ -1)
    全局_中控台_现行选中项 ＝ 超级列表框_中控台.现行选中项 ＋ 1
    弹出菜单 (菜单, , )
.如果真结束


.子程序 模型载入, 逻辑型
.局部变量 det, 字节集
.局部变量 cls, 字节集
.局部变量 rec, 字节集
.局部变量 keys, 文本型

det ＝ 读入文件 (取运行目录 () ＋ “\资源\models\ch_ppocr_server_v2.0_det_infer.onnx”)
cls ＝ 读入文件 (取运行目录 () ＋ “\资源\models\ch_ppocr_mobile_v2.0_cls_infer.onnx”)
rec ＝ 读入文件 (取运行目录 () ＋ “\资源\models\ch_ppocr_server_v2.0_rec_infer.onnx”)
keys ＝ 到文本 (读入文件 (取运行目录 () ＋ “\资源\models\ppocr_keys_v1.txt”))
全局_模型指针 ＝ Init (det, 取字节集长度 (det), cls, 取字节集长度 (cls), rec, 取字节集长度 (rec), keys, 5)
.如果真 (全局_模型指针 ＞ 100)
    调试输出 (“模型载入成功”)
    返回 (真)
.如果真结束


返回 (假)

.子程序 __启动窗口_创建完毕

.如果真 (插件注册_大漠注册 () ≠ 真)  ' 这边如果注册大漠是失败的！那就没必要在往下走了！
    返回 ()
.如果真结束

.如果真 (opencv_登录 () ≠ 真)
    调试输出 (“opencv注册失败”)
    返回 ()
.如果真结束

.如果真 (模型载入 () ≠ 真)
    调试输出 (“模型载入失败”)
    返回 ()
.如果真结束

' ↓  下面的含义是  超级列表框_中控台有几个表项数  就重新定义  全局_信息 数组成员数
重定义数组 (全局_信息, 假, 超级列表框_中控台.取表项数 ())  ' 重新定义 全局_信息的数组  防止接下去的操作   数组越标报错


.子程序 _按钮1_被单击



.子程序 _启动_被选择

' 第一步 判断线程是否已经在运行
.如果 (全局_信息 [全局_中控台_现行选中项].线程状态 ≠ “启动” 且 全局_信息 [全局_中控台_现行选中项].线程状态 ≠ “暂停” 且 全局_信息 [全局_中控台_现行选中项].线程状态 ≠ “恢复”)

    全局_信息 [全局_中控台_现行选中项].线程句柄 ＝ 线程_启动 (&主程序, 全局_中控台_现行选中项, )
    .如果 (全局_信息 [全局_中控台_现行选中项].线程句柄 ≠ 0)
        ' 线程启动成功

        ' 第一步  需要给线程状态赋值一下
        全局_信息 [全局_中控台_现行选中项].线程状态 ＝ “启动”



    .否则
        ' 线程启动失败、
        调试输出 (“线程启动失败”)
    .如果结束



.否则
    调试输出 (“当前索引” ＋ 到文本 (全局_中控台_现行选中项) ＋ “已经在运行”)
.如果结束



.子程序 _停止_被选择

.如果 (线程_销毁 (全局_信息 [全局_中控台_现行选中项].线程句柄))
    ' 给线程状态赋值
    全局_信息 [全局_中控台_现行选中项].线程状态 ＝ “停止”
    调试输出 (“窗口” ＋ 到文本 (全局_中控台_现行选中项) ＋ “号线程已停止”)


.否则
    调试输出 (“窗口” ＋ 到文本 (全局_中控台_现行选中项) ＋ “号停止线程失败”)
.如果结束


.子程序 _暂停_被选择

线程_挂起 (全局_信息 [全局_中控台_现行选中项].线程句柄)
调试输出 (“窗口” ＋ 到文本 (全局_中控台_现行选中项) ＋ “号线程已挂起”)
全局_信息 [全局_中控台_现行选中项].线程状态 ＝ “挂起”


.子程序 _恢复_被选择

线程_恢复 (全局_信息 [全局_中控台_现行选中项].线程句柄)
调试输出 (“窗口” ＋ 到文本 (全局_中控台_现行选中项) ＋ “号线程已恢复”)
全局_信息 [全局_中控台_现行选中项].线程状态 ＝ “启动”

.子程序 _全部启动_被选择
.局部变量 索引, 整数型


' 第一种加个许可证   第二种 利用全局变量来判定  或者其他

.计次循环首 (超级列表框_中控台.取表项数 (), 索引)
    .如果 (全局_信息 [索引].线程状态 ≠ “启动” 且 全局_信息 [索引].线程状态 ≠ “暂停” 且 全局_信息 [索引].线程状态 ≠ “恢复”)
        全局_信息 [索引].线程句柄 ＝ 线程_启动 (&主程序, 索引, )
        .如果 (全局_信息 [索引].线程句柄 ≠ 0)
            全局_信息 [索引].线程状态 ＝ “启动”

        .否则
            ' 线程启动失败、
            调试输出 (“线程启动失败”)
        .如果结束

    .否则
        调试输出 (“当前索引” ＋ 到文本 (索引) ＋ “已经在运行”)
    .如果结束

    处理事件 ()
    延时 (500)
.计次循环尾 ()

.子程序 _全部停止_被选择
.局部变量 索引, 整数型

.计次循环首 (超级列表框_中控台.取表项数 (), 索引)
    .如果 (线程_销毁 (全局_信息 [索引].线程句柄))
        全局_信息 [索引].线程状态 ＝ “停止”
        调试输出 (“窗口” ＋ 到文本 (索引) ＋ “号线程已停止”)

    .否则
        调试输出 (“窗口” ＋ 到文本 (索引) ＋ “号停止线程失败”)
    .如果结束


    处理事件 ()
    延时 (500)
.计次循环尾 ()

.子程序 _全部暂停_被选择
.局部变量 索引, 整数型

.计次循环首 (超级列表框_中控台.取表项数 (), 索引)
    线程_挂起 (全局_信息 [索引].线程句柄)
    调试输出 (“窗口” ＋ 到文本 (索引) ＋ “号线程已挂起”)
    全局_信息 [索引].线程状态 ＝ “挂起”

    处理事件 ()
    延时 (500)
.计次循环尾 ()

.子程序 _全部恢复_被选择
.局部变量 索引, 整数型

.计次循环首 (超级列表框_中控台.取表项数 (), 索引)
    线程_恢复 (全局_信息 [索引].线程句柄)
    调试输出 (“窗口” ＋ 到文本 (索引) ＋ “号线程已恢复”)
    全局_信息 [索引].线程状态 ＝ “启动”


    处理事件 ()
    延时 (500)
.计次循环尾 ()
