---
title: "程序集_常用功能"
weight: 23
---
.版本 2

.程序集 程序集_常用功能

.子程序 界面判定_任务栏是否打开, 逻辑型
.参数 i, 整数型
.参数 坐标, 坐标类, 参考 可空
.局部变量 序号, 整数型

序号 ＝ 全局_信息 [i].大漠.找图 (76, 67, 719, 454, “任务_任务提示.bmp|任务_开启任务.bmp”, “030303”, 0.9, 0, 坐标.x, 坐标.y)
.如果真 (序号 ≠ -1)
    .如果真 (序号 ＝ 1)
        ' 143,333
        坐标.x ＝ 坐标.x ＋ 143
        坐标.y ＝ 坐标.y － 333
    .如果真结束

    ' 代表任务栏已经打开
    返回 (真)
.如果真结束


返回 (假)

.子程序 打开_任务栏
.参数 i, 整数型

.计次循环首 (15, )
    .如果 (界面判定_任务栏是否打开 (i, ))
        返回 ()
    .否则
        全局_信息 [i].大漠.键盘_按住 (#Alt键)
        延时 (100)
        全局_信息 [i].大漠.键盘_按键 (#Q键)
        延时 (100)
        全局_信息 [i].大漠.键盘_弹起 (#Alt键)
        延时 (500)
    .如果结束

    延时 (100)
.计次循环尾 ()

.子程序 关闭_任务栏
.参数 i, 整数型

.计次循环首 (15, )
    .如果 (界面判定_任务栏是否打开 (i, ) ≠ 真)
        返回 ()
    .否则
        全局_信息 [i].大漠.键盘_按住 (#Alt键)
        延时 (100)
        全局_信息 [i].大漠.键盘_按键 (#Q键)
        延时 (100)
        全局_信息 [i].大漠.键盘_弹起 (#Alt键)
        延时 (500)
    .如果结束

    延时 (100)
.计次循环尾 ()

.子程序 界面判定_小地图, 逻辑型, , 判定小地图界面是否打开
.参数 i, 整数型
.参数 参数_坐标, 坐标类, 参考 可空
.局部变量 坐标, 坐标类

.如果真 (全局_信息 [i].大漠.找图 (84, 261, 767, 589, “小地图_寻人.bmp|小地图_寻人1.bmp”, “030303”, 0.9, 0, 坐标.x, 坐标.y) ≠ -1)
    ' 小地图的 起始位置
    参数_坐标.x ＝ 坐标.x － 3
    参数_坐标.y ＝ 坐标.y － 10
    返回 (真)
.如果真结束

返回 (假)

.子程序 打开_小地图
.参数 i, 整数型

.计次循环首 (10, )
    .如果 (界面判定_小地图 (i, ))
        返回 ()
    .否则
        全局_信息 [i].大漠.键盘_按键 (#Tab键)
    .如果结束
    延时 (500)
.计次循环尾 ()

.子程序 关闭_小地图
.参数 i, 整数型

.计次循环首 (10, )
    .如果 (界面判定_小地图 (i, ) ≠ 真)
        返回 ()
    .否则
        全局_信息 [i].大漠.键盘_按键 (#Tab键)
    .如果结束
    延时 (500)
.计次循环尾 ()

.子程序 界面判定_背包是否打开, 逻辑型
.参数 i, 整数型
.参数 背包物品识别范围, 坐标类, 参考 可空
.参数 是否道具栏, 逻辑型, 可空, 为真代表是切换到道具栏
.局部变量 背包坐标, 坐标类
.局部变量 序号, 整数型
.局部变量 切换栏识别范围, 坐标类


.如果真 (是否为空 (是否道具栏))  ' 如果未传入参数  默认参数值为真  就是切换到道具栏
    是否道具栏 ＝ 真
.如果真结束

.计次循环首 (50, )
    序号 ＝ 全局_信息 [i].大漠.找图 (47, 106, 766, 580, “背包_锦衣.bmp|背包_道具行囊.bmp”, “030303”, 0.9, 0, 背包坐标.x, 背包坐标.y)
    .如果 (序号 ≠ -1)

        ' 锁定物品栏范围
        .如果真 (序号 ＝ 0)
            ' 找到锦衣  (169,245)
            背包物品识别范围.x ＝ 背包坐标.x ＋ 169
            背包物品识别范围.y ＝ 背包坐标.y － 245
            ' 256,200
            背包物品识别范围.x1 ＝ 背包物品识别范围.x ＋ 256
            背包物品识别范围.y1 ＝ 背包物品识别范围.y ＋ 200


            ' 切换栏识别 (167,284)
            切换栏识别范围.x ＝ 背包坐标.x ＋ 167
            切换栏识别范围.y ＝ 背包坐标.y － 284
            ' (274,38)
            切换栏识别范围.x1 ＝ 切换栏识别范围.x ＋ 274
            切换栏识别范围.y1 ＝ 切换栏识别范围.y ＋ 38
    
        .如果真结束
    
        .如果真 (序号 ＝ 1)
            ' 找到行囊  (169,245)
            背包物品识别范围.x ＝ 背包坐标.x ＋ 12
            背包物品识别范围.y ＝ 背包坐标.y － 55
            ' 256,200
            背包物品识别范围.x1 ＝ 背包物品识别范围.x ＋ 256
            背包物品识别范围.y1 ＝ 背包物品识别范围.y ＋ 200


            ' 切换栏识别 (12,16)
            切换栏识别范围.x ＝ 背包坐标.x ＋ 12
            切换栏识别范围.y ＝ 背包坐标.y ＋ 16
            ' (274,38)
            切换栏识别范围.x1 ＝ 切换栏识别范围.x ＋ 274
            切换栏识别范围.y1 ＝ 切换栏识别范围.y ＋ 38
    
        .如果真结束
    
        ' 判定下是否在道具栏  切换识别




        .如果 (是否道具栏)
            ' 检查下是否在道具栏
            .如果 (全局_信息 [i].大漠.找图 (切换栏识别范围.x, 切换栏识别范围.y, 切换栏识别范围.x1, 切换栏识别范围.y1, “背包_道具按钮.bmp”, “030303”, 0.9, 0, 背包坐标.x, 背包坐标.y) ≠ -1)
                ' 当前不在道具栏
                梦幻鼠标移动 (i, 背包坐标.x, 背包坐标.y, , )
                延时 (50)
                全局_信息 [i].大漠.鼠标_左键单击 ()
                延时 (500)
            .否则
                ' 已经在道具栏了
                返回 (真)
            .如果结束
    
        .否则
            .如果 (全局_信息 [i].大漠.找图 (切换栏识别范围.x, 切换栏识别范围.y, 切换栏识别范围.x1, 切换栏识别范围.y1, “背包_行囊按钮.bmp”, “030303”, 0.9, 0, 背包坐标.x, 背包坐标.y) ≠ -1)
                ' 当前不在行囊
                梦幻鼠标移动 (i, 背包坐标.x, 背包坐标.y, , )
                延时 (50)
                全局_信息 [i].大漠.鼠标_左键单击 ()
                延时 (500)
            .否则
                ' 已经在行囊了
                返回 (真)
            .如果结束
    
        .如果结束



    .否则
        返回 (假)
    .如果结束


.计次循环尾 ()
返回 (假)


.子程序 打开_背包
.参数 i, 整数型

.计次循环首 (30, )
    .如果 (界面判定_背包是否打开 (i, , ))
        返回 ()
    .否则
        全局_信息 [i].大漠.键盘_按住 (#Alt键)
        延时 (50)
        全局_信息 [i].大漠.键盘_按键 (#E键)
        延时 (50)
        全局_信息 [i].大漠.键盘_弹起 (#Alt键)
        延时 (500)
    .如果结束

    延时 (500)
.计次循环尾 ()

.子程序 关闭_背包
.参数 i, 整数型

.计次循环首 (30, )
    .如果 (界面判定_背包是否打开 (i, , ) ≠ 真)
        返回 ()
    .否则
        全局_信息 [i].大漠.键盘_按住 (#Alt键)
        延时 (50)
        全局_信息 [i].大漠.键盘_按键 (#E键)
        延时 (50)
        全局_信息 [i].大漠.键盘_弹起 (#Alt键)
        延时 (500)
    .如果结束

    延时 (500)
.计次循环尾 ()

.子程序 界面判定_是否有对话框, 逻辑型, , 备注--后面有大用  而且代码不少
.参数 i, 整数型
.参数 是否关闭对话框, 逻辑型, 可空
.局部变量 坐标, 坐标类

.如果 (是否关闭对话框)
    .计次循环首 (50, )
        .如果 (全局_信息 [i].大漠.找图 (543, 213, 774, 422, “对话框_关闭.bmp”, “030303”, 0.9, 0, 坐标.x, 坐标.y) ≠ -1)
            梦幻鼠标移动 (i, 坐标.x ＋ 6, 坐标.y ＋ 8, , )
            延时 (50)
            全局_信息 [i].大漠.鼠标_左键单击 ()
            延时 (500)
        .否则
            跳出循环 ()
        .如果结束


        延时 (100)
    .计次循环尾 ()


.否则
    .如果真 (全局_信息 [i].大漠.找图Ex (543, 213, 774, 422, “对话框_关闭.bmp”, “030303”, 0.9, 0) ≠ “”)
        返回 (真)
    .如果真结束

.如果结束
返回 (假)
