---
title: "程序集_鼠标移动"
weight: 23
---
.版本 2
.支持库 spec

.程序集 程序集_鼠标移动

.子程序 梦幻鼠标移动
.参数 i, 整数型
.参数 参数_移动x, 整数型
.参数 参数_移动y, 整数型
.参数 精准度, 整数型, 可空
.参数 未找到鼠标直接返回, 逻辑型, 可空
.局部变量 当前鼠标位置x, 整数型
.局部变量 当前鼠标位置y, 整数型
.局部变量 鼠标位置是否找到, 整数型
.局部变量 临时位置x, 整数型
.局部变量 临时位置y, 整数型
.局部变量 画面卡顿移动, 逻辑型
.局部变量 卡顿次数, 整数型
.局部变量 x修正位置, 文本型
.局部变量 x逆向修正次数, 整数型
.局部变量 x正向修正次数, 整数型
.局部变量 y修正位置, 文本型
.局部变量 y逆向修正次数, 整数型
.局部变量 y正向修正次数, 整数型
.局部变量 未找到鼠标次数, 整数型
.局部变量 临时字库, 整数型
.局部变量 索引, 整数型

临时字库 ＝ 全局_信息 [i].大漠.字库_取序号 ()
全局_信息 [i].大漠.字库_置序号 (#字库_鼠标)
.如果真 (是否为空 (精准度))
    精准度 ＝ 3
.如果真结束




x修正位置 ＝ “”
.如果 (参数_移动x ≥ 400)
    .如果 (参数_移动x ≥ 532)
        .如果 (参数_移动x ≥ 730)
            全局_信息 [i].鼠标偏移x ＝ -70
        .否则
            全局_信息 [i].鼠标偏移x ＝ -21 ＋ 全局_信息 [i].鼠标_X轴高位修正
            x修正位置 ＝ “高”
        .如果结束

    .否则
        全局_信息 [i].鼠标偏移x ＝ -18 ＋ 全局_信息 [i].鼠标_X轴中部修正
        x修正位置 ＝ “中”
    .如果结束

.否则
    .如果 (参数_移动x ≤ 50)
        全局_信息 [i].鼠标偏移x ＝ 全局_信息 [i].鼠标_X轴零点修正 ＋ 60
    .否则
        全局_信息 [i].鼠标偏移x ＝ 全局_信息 [i].鼠标_X轴零点修正 ＋ 10
        x修正位置 ＝ “零”

    .如果结束

.如果结束

y修正位置 ＝ “”
.如果 (参数_移动y ≥ 300)
    .如果 (参数_移动y ≥ 460)
        .如果 (参数_移动y ≥ 580)
            全局_信息 [i].鼠标偏移Y ＝ -70
        .否则
            全局_信息 [i].鼠标偏移Y ＝ -21 ＋ 全局_信息 [i].鼠标_Y轴高位修正
            y修正位置 ＝ “高”
        .如果结束

    .否则
        全局_信息 [i].鼠标偏移Y ＝ -19 ＋ 全局_信息 [i].鼠标_Y轴中部修正
        y修正位置 ＝ “中”
    .如果结束

.否则
    .如果 (参数_移动y ≥ 90)
        全局_信息 [i].鼠标偏移Y ＝ 全局_信息 [i].鼠标_Y轴零点修正 － 3
        y修正位置 ＝ “零”
    .否则
        全局_信息 [i].鼠标偏移Y ＝ 40
        y修正位置 ＝ “”
    .如果结束

.如果结束




调试输出 (“偏移值” ＋ 到文本 (全局_信息 [i].鼠标偏移x))


全局_信息 [i].大漠.鼠标移动 (参数_移动x ＋ 全局_信息 [i].鼠标偏移x, 参数_移动y ＋ 全局_信息 [i].鼠标偏移Y)  ' 这边已经移动了  绝对移动
延时 (500)
.计次循环首 (200, 索引)
    ' 查找一下鼠标的位置
    ' 鼠标取色的颜色   2ae9e2-202020|28f4f0-202020|209ce0-202020|18ccc0-202020|58f0f0-202020|68c4d0-202020


    ' 暂时替代   这个一天0.3   联系志翔 开下账号  QQ 878033713
    .计次循环首 (5, )
        鼠标位置是否找到 ＝ 全局_信息 [i].大漠.找字Fast (0, 22, 804, 596, “鼠标”, “2ae9e2-202020|28f4f0-202020|209ce0-202020|18ccc0-202020|58f0f0-202020|68c4d0-202020”, 0.9, 当前鼠标位置x, 当前鼠标位置y)  ' 字  字库？？？
        .' 如果真 (鼠标位置是否找到 ＝ -1)  ' 注意哈  这边是没找到哈~~  没找到 才执行
            ' ' ----执行opencv模板匹配(找图)
            ' ' 为何不直接用?   Opencv有一定损耗性能占用 多线程情况下!
            ' 调试输出 (“触发opencv识别”)
            鼠标位置是否找到 ＝ opencv_找图 (i, 3, 21, 803, 598, “鼠标_针尖.bmp|鼠标_针尖1.bmp|鼠标_针尖2.bmp”, 0.6, 当前鼠标位置x, 当前鼠标位置y)
        .如果真结束




        .如果 (鼠标位置是否找到 ≠ -1)  ' 防止没找到的 !  调用opencv识别
            跳出循环 ()
        .否则
            .如果真 (未找到鼠标直接返回)
                全局_信息 [i].大漠.字库_置序号 (临时字库)
                返回 ()
            .如果真结束

        .如果结束



    .计次循环尾 ()


    .如果 (鼠标位置是否找到 ≠ -1)
        ' 在找到鼠标以后处理
        未找到鼠标次数 ＝ 1
        .如果 (临时位置x ≠ 当前鼠标位置x 或 临时位置y ≠ 当前鼠标位置y 或 画面卡顿移动 ＝ 真)
            .如果真 (索引 ＞ 3)
                临时位置x ＝ 当前鼠标位置x
                临时位置y ＝ 当前鼠标位置y

            .如果真结束

            ' 判断一下  鼠标是不是在传进来参数要求的范围内
            .如果 (取绝对值 (当前鼠标位置x － 参数_移动x) ≤ 精准度 且 取绝对值 (当前鼠标位置y － 参数_移动y) ≤ 精准度)
                调试输出 (“当前鼠标位置正确”)
                全局_信息 [i].大漠.字库_置序号 (临时字库)
                返回 ()
            .否则

                画面卡顿移动 ＝ 假
                ' ？？？？？？？？不在范围中
                ' X轴移动处理  什么时候处理？ 怎么处理？
                .如果真 (取绝对值 (当前鼠标位置x － 参数_移动x) ＞ 精准度)  ' 第一个问题 什么时候处理x轴  当用户要移动的位置  和  查找到得屏幕鼠标位置 不在精确度范围内  进行处理
                    ' 怎么处理？？？  相对移动
                    ' 第一步 是不是得确定下左右方向
                    .如果 (当前鼠标位置x ＞ 参数_移动x)
                        .如果 (x逆向修正次数 ≥ 5)
                            x逆向修正次数 ＝ 1
                            .如果真 (x修正位置 ＝ “零”)
                                全局_信息 [i].鼠标_X轴零点修正 ＝ 全局_信息 [i].鼠标_X轴零点修正 － 1
                            .如果真结束
                            .如果真 (x修正位置 ＝ “中”)
                                全局_信息 [i].鼠标_X轴中部修正 ＝ 全局_信息 [i].鼠标_X轴中部修正 － 1
                            .如果真结束
                            .如果真 (x修正位置 ＝ “高”)
                                全局_信息 [i].鼠标_X轴高位修正 ＝ 全局_信息 [i].鼠标_X轴高位修正 － 1
                            .如果真结束

                        .否则
                            x逆向修正次数 ＝ x逆向修正次数 ＋ 1
                            x正向修正次数 ＝ 1
                        .如果结束

                    .否则
                        .如果 (x正向修正次数 ≥ 5)
                            x正向修正次数 ＝ 1
                            .如果真 (x修正位置 ＝ “零”)
                                全局_信息 [i].鼠标_X轴零点修正 ＝ 全局_信息 [i].鼠标_X轴零点修正 ＋ 1
                            .如果真结束
                            .如果真 (x修正位置 ＝ “中”)
                                全局_信息 [i].鼠标_X轴中部修正 ＝ 全局_信息 [i].鼠标_X轴中部修正 ＋ 1
                            .如果真结束
                            .如果真 (x修正位置 ＝ “高”)
                                全局_信息 [i].鼠标_X轴高位修正 ＝ 全局_信息 [i].鼠标_X轴高位修正 ＋ 1

                            .如果真结束

                        .否则
                            x正向修正次数 ＝ x正向修正次数 ＋ 1
                            x逆向修正次数 ＝ 1
                        .如果结束

                    .如果结束
                    .如果 (当前鼠标位置x ＞ 参数_移动x)
                        .如果 (取绝对值 (当前鼠标位置x － 参数_移动x) ≥ 5)
                            全局_信息 [i].大漠.鼠标_相对移动 (-3, 0)
                        .否则
                            全局_信息 [i].大漠.鼠标_相对移动 (-1, 0)
                        .如果结束


                    .否则
                        .如果 (取绝对值 (当前鼠标位置x － 参数_移动x) ≥ 5)
                            全局_信息 [i].大漠.鼠标_相对移动 (3, 0)
                        .否则
                            全局_信息 [i].大漠.鼠标_相对移动 (1, 0)
                        .如果结束

                    .如果结束

                .如果真结束



                ' Y轴处理？？？？
                .如果真 (取绝对值 (当前鼠标位置y － 参数_移动y) ＞ 精准度)
                    ' ' 怎么处理？？？  相对移动
                    ' ' 第一步 是不是得确定下上下方向

                    .如果 (当前鼠标位置y ＞ 参数_移动y)
                        .如果 (y逆向修正次数 ≥ 5)  ' 主要不能相对移动一次 就加1   5次  10次
                            y逆向修正次数 ＝ 1
                            .如果真 (y修正位置 ＝ “零”)
                                全局_信息 [i].鼠标_Y轴零点修正 ＝ 全局_信息 [i].鼠标_Y轴零点修正 － 1
                            .如果真结束
                            .如果真 (y修正位置 ＝ “中”)
                                全局_信息 [i].鼠标_Y轴中部修正 ＝ 全局_信息 [i].鼠标_Y轴中部修正 － 1
                            .如果真结束
                            .如果真 (y修正位置 ＝ “高”)
                                全局_信息 [i].鼠标_Y轴高位修正 ＝ 全局_信息 [i].鼠标_Y轴高位修正 － 1
                            .如果真结束

                        .否则
                            y逆向修正次数 ＝ y逆向修正次数 ＋ 1
                            y正向修正次数 ＝ 1
                        .如果结束

                    .否则
                        .如果 (y正向修正次数 ≥ 5)
                            y正向修正次数 ＝ 1
                            .如果真 (y修正位置 ＝ “零”)
                                全局_信息 [i].鼠标_Y轴零点修正 ＝ 全局_信息 [i].鼠标_Y轴零点修正 ＋ 1
                            .如果真结束
                            .如果真 (y修正位置 ＝ “中”)
                                全局_信息 [i].鼠标_Y轴中部修正 ＝ 全局_信息 [i].鼠标_Y轴中部修正 ＋ 1
                            .如果真结束
                            .如果真 (y修正位置 ＝ “高”)
                                全局_信息 [i].鼠标_Y轴高位修正 ＝ 全局_信息 [i].鼠标_Y轴高位修正 ＋ 1

                            .如果真结束

                        .否则
                            x正向修正次数 ＝ x正向修正次数 ＋ 1
                            y逆向修正次数 ＝ 1
                        .如果结束

                    .如果结束
                    .如果 (当前鼠标位置y ＞ 参数_移动y)

                        .如果 (取绝对值 (当前鼠标位置y － 参数_移动y) ≥ 5)
                            全局_信息 [i].大漠.鼠标_相对移动 (0, -3)
                        .否则
                            全局_信息 [i].大漠.鼠标_相对移动 (0, -1)
                        .如果结束



                    .否则
                        .如果 (取绝对值 (当前鼠标位置y － 参数_移动y) ≥ 5)
                            全局_信息 [i].大漠.鼠标_相对移动 (0, 3)
                        .否则
                            全局_信息 [i].大漠.鼠标_相对移动 (0, 1)
                        .如果结束

                    .如果结束





                .如果真结束

            .如果结束

        .否则
            ' 画面卡顿   解决因画面  卡顿  来回蹦跶
            调试输出 (“画面卡顿”)
            ' 鼠标 位置一直卡着    导致此处一直卡点
            .如果 (卡顿次数 ≥ 30)
                画面卡顿移动 ＝ 真  ' 强行移动下鼠标
                卡顿次数 ＝ 1
                调试输出 (“画面卡顿触发强行移动”)

            .否则

                调试输出 (“画面卡顿次数:” ＋ 到文本 (卡顿次数))
                卡顿次数 ＝ 卡顿次数 ＋ 1

            .如果结束


        .如果结束



    .否则
        调试输出 (“当前鼠标位置未找到”)
        .如果 (未找到鼠标直接返回)
            全局_信息 [i].大漠.字库_置序号 (临时字库)
            返回 ()
        .否则
            .如果 (未找到鼠标次数 ≥ 5)
                调试输出 (“未找到鼠标位置”)
                全局_信息 [i].大漠.字库_置序号 (临时字库)
                返回 ()
            .否则
                未找到鼠标次数 ＝ 未找到鼠标次数 ＋ 1
            .如果结束

        .如果结束

    .如果结束


    处理事件 ()
.计次循环尾 ()
